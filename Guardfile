# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'jekyll' do
  # watch(/(.*)/)
  watch(%r{^_layouts/.+\.html$})
  watch(%r{^_posts/.+\.md$})
  watch(%r{^css/.+\.css$})
  watch('index.html')
end

